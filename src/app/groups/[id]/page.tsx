import SingleGroup from "../_components/SingleGroup"

const ViewGroupMoments = async ({ params }: { params: { id: string } }) => {
  return <SingleGroup params={params} />
}

export default ViewGroupMoments
